# playground

Scratch space for my own toy / experiment scripts (not part of cyphys, newton, or mujoco_warp).
Uses the shared conda `sim` env, so `import my_sim`, `import newton`, `import warp`, etc. all work.

